# ---- Wails / 前端构建产物 ----
frontend/dist
node_modules
frontend/package.json.md5

# ---- 编译产物(wails build / go build 输出) ----
build/bin/
# 发布产物走 GitHub Releases,不提交二进制(历史遗留的 release/ 文件不再跟踪)
release/
*.app
*.dmg
*.exe
/gbt32960-simulator
/gbt32960-simulator.exe

# ---- Go 测试 / 覆盖率产物 ----
*.test
*.out
coverage.*

# ---- 根目录调试截图(agent 视觉走查产物) ----
/*.png

# ---- IDE / 编辑器 ----
.idea/
.vscode/

# ---- macOS ----
.DS_Store

# ---- AI / Agent 开发文档产物 ----
# .codegraph 是指向 ~/.omo 的符号链接(代码索引,agent 产物)
.codegraph
.claude/
.opencode/
.codex/
.cursor/
.omo/
.agents/
.cache/

# ---- SDD 执行台账(agent 产物) ----
.superpowers/

# ---- playwright-mcp 调试产物(截图/日志) ----
.playwright-mcp/

# 更新签名私钥(离线保管,绝不入库)
update-signing*.key
