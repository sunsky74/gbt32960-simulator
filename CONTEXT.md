# GB/T 32960 模拟器

GB/T 32960 客户端模拟器的桌面应用(Wails v2 + Vue 3)。本文件是项目术语表——只收录本项目特有的概念,通用编程概念不在此列。

## 应用更新

**自动检查**:
启动后自动触发(默认开、24h 冷却)、失败不打扰的更新检查,可在设置中关闭。
_Avoid_: 静默检查、后台检查、定时检查

**跳过版本**:
用户对某个具体版本做出的"不再提示"持久化决定。自动检查对其静默,手动检查仍展示;更高版本发布后该决定自动失效。
_Avoid_: 忽略版本、屏蔽版本、忽略此更新

**更新产物**:
某次 Release 中为当前平台准备、用于替换应用自身的文件(macOS 为整个 `.app` 归档,Windows 为 exe,Linux 为二进制)。
_Avoid_: 安装包、补丁包、升级包

**安装并重启**:
更新流程的最终动作:替换程序主体并自动拉起新版本;用户配置与数据不受影响。
_Avoid_: 升级、立即更新

**helper 模式**:
应用以特殊参数再次启动自身形成的短驻进程:等待父进程退出后执行替换,失败时回滚并拉起旧版本。
_Avoid_: 更新器、守护进程、launcher
